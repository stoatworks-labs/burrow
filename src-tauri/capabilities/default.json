{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Burrow's main window. The demo window is deliberately absent from `windows`: it loads a local page that needs no IPC, and granting it none is what keeps a bundled demo unable to call an install command.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
