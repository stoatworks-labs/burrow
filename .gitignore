node_modules/
dist/
target/
src-tauri/target/
src-tauri/gen/

# The privileged helper is compiled, per target triple, by
# scripts/sync-assets.sh or by the release workflow. Never committed.
src-tauri/bin/

# A working copy the mock backend and the screenshot script need beside the
# built front end. The app itself reads the baked catalogue from its resources.
public/catalog.json

# Release-time artefacts. The workflow writes these at the workspace root, and
# so does anyone running the same commands locally to check them.
*.app.tar.gz
*.app.tar.gz.sig
latest.json
release-body.md

.DS_Store
