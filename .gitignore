node_modules/
dist/
target/
src-tauri/target/
src-tauri/gen/

# The privileged helper is compiled, per target triple, by
# scripts/sync-assets.sh or by the release workflow. Never committed.
src-tauri/bin/

# A working copy the mock backend and the screenshot script need beside the
# built front end. The app itself reads the baked catalogue from its resources.
public/catalog.json

.DS_Store
